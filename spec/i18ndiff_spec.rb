require 'spec_helper'

describe I18ndiff do
  it 'has a version number' do
    expect(I18ndiff::VERSION).not_to be nil
  end

  it 'has an old file'

  it "has a new file to compare to the old file"

  it "outputs the stuff in the new file that is not in the old file"

end
